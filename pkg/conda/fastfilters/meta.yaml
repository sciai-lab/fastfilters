package:
  name: fastfilters
  version: "0.0"

source:
  git_url: https://github.com/svenpeter42/fastfilters
  git_tag: ac3486c9a559c016a5c2146eb40f9f1ac0e0ff97

build:
  {% set build_num = 1 %}
  number: {{ build_num }}
  string: py{{CONDA_PY}}_{{build_num}}_g{{GIT_FULL_HASH[:7]}}

requirements:
  build:
    - gcc 4.8.5 # [linux]
    - gcc 4.8.5 # [osx]
    - python 2.7*|3.5*
    - python {{PY_VER}}*

  run:
    - python {{PY_VER}}*